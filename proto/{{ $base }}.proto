syntax = "proto3";

package {{ $base }};

option go_package = ".;{{ $base }}";

service {{ $base }} {
  rpc Service(Request) returns (Response);
}

message Request {
  string Message = 1;
}

message Response {
  string Message = 1;
}
